// Base URL de la API de Cosmos. "" = mismo origen (útil cuando la API sirve esta web como estático).
// Si despliegas esta web por separado, cámbialo por la URL absoluta de tu instancia de la API,
// por ejemplo: "https://cosmos-api.tudominio.com"
window.COSMOS_CONFIG = {
  apiBase: ""
};
